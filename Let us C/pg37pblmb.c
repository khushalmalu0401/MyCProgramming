#include<stdio.h>
#include<math.h>
int main()
{
    int x,y,r,Theta;
    printf("Enter the cartesian cordinates(x,y):");
    scanf("%d %d", &x, &y);
    // int p = x*x + y*y;
    // r = sqrt(p);
    // printf("%d", r);
    int z=y/x;
    double atan(double z);
    printf("%d", z);
    return 0;
}
    